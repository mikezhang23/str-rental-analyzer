name: Keep Streamlit Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # Every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -sL -o /dev/null -w "%{http_code}" https://str-rental-analyzer.streamlit.app/
