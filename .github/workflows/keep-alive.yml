name: Keep Streamlit Alive

on:
  schedule:
    - cron: '*/10 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -s -o /dev/null -w "%{http_code}" --max-redirs 10 -L https://str-rental-analyzer.streamlit.app/ || true
